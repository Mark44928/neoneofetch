language: bash
sudo: required

os:
    - linux
    - osx

before_install:
    - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update; fi
    - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install screenresolution; fi

script:
    - time ./neoneofetch --travis -v
    - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then shellcheck neoneofetch; fi
    # Check for lines longer than 100 chars.
    - if grep '.\{101\}' neoneofetch; then (exit 1); else (exit 0); fi
